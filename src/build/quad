// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ---- //
// quad //
// ---- //

#define quad_wrap_target 0
#define quad_wrap 3
#define quad_pio_version 0

static const uint16_t quad_program_instructions[] = {
            //     .wrap_target
    0xe000, //  0: set    pins, 0                    
    0xe000, //  1: set    pins, 0                    
    0xe003, //  2: set    pins, 3                    
    0xe003, //  3: set    pins, 3                    
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program quad_program = {
    .instructions = quad_program_instructions,
    .length = 4,
    .origin = -1,
    .pio_version = quad_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config quad_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + quad_wrap_target, offset + quad_wrap);
    return c;
}
#endif

